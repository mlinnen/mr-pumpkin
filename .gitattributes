# Squad: .ai-team/ never ships to preview/main.
# merge=ours keeps the current branch's version (deleted on preview) when merging from dev.
# Note: requires local driver: git config merge.ours.driver true
.ai-team/** merge=ours
