# Squad: .ai-team/ and .ai-team-templates/ are dev-only.
# These files are tracked on dev but explicitly excluded from preview/main via git rm.
# The GitHub PR merge process doesn't execute custom merge drivers, so manual branch management is necessary.
# See README.md for team directive on branch management.
