# Orchestration Log: Mylo — Issue 99 Batch
**Date:** 2026-05-13T15:40:59Z  
**Agent:** Mylo (Tester)  
**Status:** Test scaffolding & CI infrastructure prepared

## Spawn Manifest Entry
- **Issue(s):** #99 (Test infrastructure), #109, #110, #111
- **Branch:** `squad/99-mylo-tests`
- **Status:** Test scaffolding created, CI workflow added

## Work Summary

### Deliverables

1. **Test Suite Scaffolding**
   - `tests/test_transitions.py` — state transition tests (skipped, awaiting stable API)
   - `tests/test_commands.py` — command routing and handling tests (skipped)
   - `tests/test_recording.py` — timeline recording and playback tests (skipped)
   - `tests/conftest.py` — pytest configuration with lightweight pygame shim

2. **CI/CD Infrastructure**
   - `.github/workflows/ci-tests.yml` — new GitHub Actions workflow
   - Headless environment setup: `SDL_VIDEODRIVER=dummy`
   - Python environment: pytest discovery and execution
   - Support for OS matrix (Linux) with optional SDL2 system libraries

3. **Decision Document** — `.squad/decisions/inbox/mylo-issue99.md`
   - Test strategy for pygame-dependent code
   - CI infrastructure blockers (SDL libs, xvfb, pygame mocks)
   - Recommendations for mock fixtures and headless testing

### Files Modified/Created
- `tests/` directory scaffolding — new
- `.github/workflows/ci-tests.yml` — new
- `.squad/decisions/inbox/mylo-issue99.md` → merged into decisions.md

## History Updated
- `.squad/agents/mylo/history.md` — appended Issue 99 test infrastructure work summary

## Blockers (For Jinx Approval)

1. **SDL2 Dependency for CI Runners**
   - pygame requires SDL2 system libraries (libasound2, libsdl2-dev, etc.)
   - Options:
     a) Mock pygame in unit tests (no CI deps needed)
     b) Install SDL2 packages in GitHub Actions runner
     c) Use xvfb for headless X11 display

2. **pygame in CI Environment**
   - Current approach: lightweight shim to avoid import errors
   - Recommended: either mock pygame or move rendering tests to integration suite
   - Needs decision: should unit tests exercise pygame rendering, or only state/logic?

3. **Cross-Platform CI Matrix**
   - Currently scaffolded for Linux only
   - Windows/macOS runners require SDL2 installation paths

## Next Steps

1. **Implement concrete tests** (when PumpkinFace API is stable)
   - State transition tests exercising set_expression()
   - Command handling tests for valid/invalid commands
   - Recording playback tests validating timeline execution

2. **Resolve CI blockers** (awaiting Jinx decision)
   - Choose pygame handling strategy (mock vs install)
   - Add Windows/macOS to CI matrix if needed
   - Configure SDL_VIDEODRIVER=dummy for all runners

3. **Pytest execution**
   - Run locally: `pytest tests/` to verify scaffolding
   - CI run: GitHub Actions on push to squad/99-mylo-tests branch
   - Fix any import or fixture issues discovered in CI

## Coordination Notes
- Requires Vi's engine modules (command_router, adapters) for integration tests
- Requires Ekko's expression API finalization for state transition tests
- Parallel work: scaffold tests while backend/graphics implementations proceed
