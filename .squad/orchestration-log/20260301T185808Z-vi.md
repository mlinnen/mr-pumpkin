# Orchestration: Vi WebSocket upload_timeline Fix

**Timestamp:** 2026-03-01T18:58:08Z  
**Agent:** Vi (Backend Dev)  
**Mode:** Background  
**Status:** Complete

## Work Summary

Fixed WebSocket upload_timeline handler in `pumpkin_face.py` to intercept `upload_timeline` commands before routing to command_router and handle inline JSON format.

### Changes Made

- **File:** `pumpkin_face.py`
- **Handler:** `_ws_handler()` now intercepts `upload_timeline` messages
- **Format:** Accepts inline message format: `upload_timeline <filename> <json_string>`
- **Behavior:** Parses filename and JSON directly, calls `file_manager.upload_timeline()` without command routing

### Test Updates

- Updated integration test to verify WebSocket upload_timeline with inline JSON format

### Decision Record

- Wrote decision document: `.squad/decisions/inbox/vi-ws-upload-timeline-fix.md`
- Rationale: WebSocket's single-message model incompatible with TCP's multi-step handshake protocol
- Security: Path separators rejected, invalid JSON caught and returned as ERROR response

## Integration Notes

- WebSocket clients can now upload timeline files in single message
- TCP clients unaffected (continue using multi-step protocol)
- Both protocols now functional with their respective transport designs
